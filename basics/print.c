#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

// C has no concept of strings, it uses char arrays terminated by a sentinel character \0
int main() {

	// Standard
	printf();

	// Can choose where this goes to - stdout or stderr
	sprintf();
}